import { RegisterForm } from '@/components/auth/RegisterForm';

export default function RegistracePage() {
  return (
    <section className="page-section flex items-center justify-center min-h-[calc(100vh-72px)]">
      <RegisterForm />
    </section>
  );
}
